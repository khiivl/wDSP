# 🔬 Дослідження: змішування навігації, гучність і BitPerfect (20.09.2026)

> 🎯 **Навіщо.** Власник: звук системи, тракт і платформні «сокири» — предмет wDSP, бо ми головна програма над звуком
> у модифікованій системі. Тут зібрано все, що знайдено за день, з доказами, і варіанти рішень.
> 📌 Провенанс позначено біля кожного пункту: **дріт** (наш вимір), **код** (декомпілят, прочитаний нами),
> **звіт** (опитування тестерів), **реверс Дж** (його висновок, нами не переміряний).

## 0. Джерела

| що | звідки |
|---|---|
| три опитування 06.09 | `004121` haiwai + BitPerfect **v5.1**; `002121` jitu2 + **v4.17**; `004121` jitu2 **без модуля** |
| опитування апарата, що не завантажився | 07.09, `sp9863a`/UIS8581, MCU `QF30…011021`, BD37534, аналоговий тракт |
| сам модуль | `QF_BitPerfect.module.v5.3-Universal.zip` — `customize.sh`, `service.sh`, обидва профілі, `common/` |
| фреймворк | `D:\UIS_android\qf_platform_framework` — `QFAudioService`, `AK7738VolumeManager`, `McuManagerService` |
| еталон 7862 | заводське дерево `D:\Release\update(2)\vendor\vendor` = розділ vendor стенда (звірено md5) |
| стенд власника | `002121`, BU32107, I2S, v5.3 встановлено (`service.sh` md5 збігається з роздачею) |

## 1. Що доведено

1. **Платформа змішує по драбинці від «типової» гучності, а не від поточної.** `QFAudioService`: база —
   `AudioSystem.getDefaultStreamVolume(3)` (= `persist.sys.main_volume`), ціль — `getVolumeMixMappedForArm(base, ratio)`:
   `ratio ≥ 90` → **сама типова гучність (приглушення немає)**, `≥80` → 8, `≥70` → 7, `≥60` → 6, `≥50` → 5 … Після
   підказки рівень повертається **в типову**, а не в те, що слухала людина. *(код)*
2. **На стенді власника `navi_remix_ratio = 90`** — тобто змішування вимкнене арифметикою платформи, а якщо слухати
   тихіше за типову, підказка ще й піднімає музику. *(дріт)*
3. **Плоска крива музики вбиває приглушення.** v4.17: `AUDIO_STREAM_MUSIC` = `FULL_SCALE`, TTS придушено до −4 дБ →
   вісім кроків приглушення не дають жодного децибела. **v5.1 уже виправлено**: градуйована крива музики (0/−96 …
   100/0) і TTS на повній шкалі; у звіті видно приглушення 15 → 8 ≈ −19 дБ. *(звіт)*
4. **«Занадто гучно на радіо» — не модуль.** У тюнера своя гучність по джерелу (`sys.radio.vol` = 5 і 8 на двох
   апаратах), підказка йде андроїдним боком близько нуля. Прикручене радіо плюс підказка на нулі = «гримить». *(звіт)*
5. **Сторож гучності модуля скасовує саме змішування.** Вічний цикл раз на 3 с: якщо індекс гучності медіа
   **дорівнює 9** і `persist.sys.navi_state` ≠ `true` — поставити 15. Але 9 — це ціль змішування при типовій гучності 9
   (значення на більшості апаратів), а `navi_state` **порожній на всіх п'яти** обстежених апаратах, тож запобіжник не
   спрацьовує ніколи. На завантаженні жорсткіше: кожні 0.3 с протягом 25 с відновлює 15 при будь-якому іншому
   значенні. *(код + звіт + дріт)*
6. **Ціна сторожа:** повний старт ART (`com.android.commands.media.Media`) **раз на ~3 с вічно** (≈29 000 на добу) і
   ≈80 стартів у перші 25 с завантаження — заради одного читання індексу. *(дріт: логер, 5 стартів за 12 с)*
7. **Витоку пам'яті від цього не видно.** Стенд, 13 год роботи циклу, замір кожні 5 хв протягом 30 хв: system_server
   512–525 МБ (коливання без нахилу), потоки 166 і дескриптори 330 **незмінні**, вільної пам'яті 3.7 ГБ з 5.8 ГБ,
   вбивств за браком пам'яті немає. *(дріт)*
8. **v5.3 не дає завантажитись на UIS8581.** Інсталятор обирає профіль лише за чипом і `use.i2s`, а копіює файли з
   образу ums512; у 8581 немає ні `audio_pcm.xml`, ні `audio_route.xml`, ні `audio_config.xml`, ні теки
   `audio_params/sprd` — і сім чужих файлів параметрів AGDSP читає vendor init задовго до лаунчера.
   *(звіт + код інсталятора)*
9. **Мовчазний TTS — це стан, не конфігурація.** «TTS output not available» у політиці — **заводське** (порту
   `AUDIO_OUTPUT_FLAG_TTS` немає в жодній заводській політиці обох платформ і в жодному профілі модуля); крива TTS
   модуля на динаміку — повна шкала, а динамік — єдиний доступний вихід. Лишається випадок 14.09: HAL тримав `pcm3p`
   у `SETUP`, і все, що туди маршрутизувалося, йшло в нікуди. *(дріт)*
10. **Хаба AK ні в кого немає** — 21 звіт, у всіх `hub = none`; тож гілки з `AK7738VolumeManager` (приглушення в чіпі,
    атенюатор від нашого повідомлення 24 з підпунктом 2) — спадок старішого заліза. *(звіт)*

## 2. Варіанти рішень

### A. Модуль (не наш код, але наш предмет)

1. 🔴 **Не писати персисти гучності взагалі** (наказ власника 20.09). Платформа рахує від них; прип'ята стеля
   позбавляє змішування сенсу, а повернення «в типову» після підказки й так перезаписує вибір людини.
2. **Сторож — на події, не на опит**: завантаження і прокидання, коротке вікно, далі мовчати. Подія, а не значення.
3. Якщо стеження лишається — **відрізняти перехід від стану** і ніколи не втручатися, поки говорить підказка.
   Ознака підказки — **не** `navi_state` (порожній), а живий трек навігатора або рядок платформи в лозі.
4. **Профіль під 8581 — з її власних заводських файлів.** Тимчасовий запобіжник до того: відмова ставитись, якщо в
   заводському `/vendor/etc` немає `audio_pcm.xml` і теки `audio_params/sprd`.
5. **Прибрати запуск віртуальної машини**: індекс гучності можна не читати взагалі (подієвий підхід), а якщо треба —
   брати те, що доступне без Java (`tinymix`, пропи, `/proc/asound`), а не `media volume --get` двадцять разів на хвилину.
6. Крива музики: у v5.1 вже градуйована — тримати її, не повертатися до `FULL_SCALE`.

### B. Для власника й тестерів просто зараз, без нової збірки

- **`persist.sys.navi_remix_ratio ≤ 80`**, інакше платформа не приглушує взагалі (90 і вище = вимкнено).
- Не тримати гучність медіа на стелі: тоді платформі є куди і опускати, і піднімати.
- Скарга «тихі підказки на плеєрах» — стара версія модуля (v4.17 і раніші), лікується оновленням.

### C. Для wDSP

1. Стрибки гучності (0 → 12, повернення «в типову») **своєму коду не приписувати** — це платформа.
2. **Вимір мікрофоном під час підказки недійсний**: на хаб-апаратах приглушення взагалі в чіпі, на решті — індексом.
   Вікно підказки в вимірах ігнорувати.
3. ❓ Відкрите питання власникові: чи робити приглушення самим (ми маємо і рівень, і MCU) замість платформної драбинки.

## 3. Чого бракує

| питання | чим закриється |
|---|---|
| що в 8581 замість відсутніх файлів; чи інша там робота зі звуком | прошивка 8581 (качається) → порівняння переліку, політик, фірмових файлів QF і самого HAL із нашим еталоном 7862 |
| чому мовчить TTS | знімок **у момент** мовчання: стан `pcm3p`, хто власник, помилки HAL, живі треки |
| чи є витік пам'яті на довшій дистанції | замір нахилу за годину-дві (перші 30 хв — без нахилу) |
| чи справді драбинка поводиться як у коді | короткий прогін на стенді зі звуком: `ratio` 60 і 90, підказка, знімок індексу |

Пов'язані документи: [platform/08-VOLUME-AND-SOURCES.md](platform/08-VOLUME-AND-SOURCES.md) розділ 6 (реєстр сокир),
[platform/09-NAVIGATION-AND-BITPERFECT.md](platform/09-NAVIGATION-AND-BITPERFECT.md) 4-ter і 10 (криві й драбинка),
[platform/10-BITPERFECT-MODULE.md](platform/10-BITPERFECT-MODULE.md) 9–11 (8581, TTS, сторож).

## 4. Еталонний документ про сліпе калібрування — що з нього беремо (прочитано 20.09.2026)

Джерело: `D:\Downloads\Алгоритмічне та апаратне калібрування мікрофона та акустики без еталона.pdf`
(«Спільне сліпе калібрування некаліброваного мікрофона та акустики автомобіля», платформа UIS7862).
Нижче — лише те, що впирається в наш код, і де ми з ним розходимося.

### 4.1 Головна теза, під якою наш підхід стоїть правильно
Мікрофон — **єдине спільне для всіх каналів**. Кімната змінюється від динаміка до динаміка, капсуль ні. Тому задача
розв'язна саме через багатоканальність (MISO): спільна частина відгуків усіх динаміків — це мікрофон.

### 4.2 Де наша математика розходиться з документом

| питання | документ | у нас зараз |
|---|---|---|
| як оцінювати мікрофон | **логарифмічне усереднення** спектрів усіх каналів, тоді відняти теоретичну огинаючу (cabin gain унизу + плавний спад угорі); решта — мікрофон | **найкращий канал у кожній смузі** (максимум) плюс **сліпа таблиця монтажу** |
| якір на низу | теорія **12 дБ/окт** нижче перехідної частоти; перехідна ≈ 565 / (найбільший розмір салону у футах); на практиці підйом починається вище (70–90 Гц, іноді 250–350) і ніколи не дотягує до 12 через витоки | фіксовані **6 дБ/окт нижче 80 Гц**, незалежно від кузова |
| спад замість підйому на 20–60 Гц | доказ фільтра високих частот **у мікрофонному тракті** — і підстава компенсувати | так само, але без застереження, що якір дійсний **лише в закритому салоні** |
| кімната проти мікрофона | провал, що є **в усіх каналах** — мікрофон; провал в одному — кімната (гребінка рухається з положенням, резонанс капсуля стоїть) | такої перевірки немає; верх малює таблиця монтажу |
| верх | гострі високодобротні піки вище 10 кГц — резонанси капсуля, їх **зрізати**; загальний спад — частина теоретичної огинаючої | таблиця додає +3.5 / +7.0 / +10.0 дБ на 8 / 12.5 / 20 кГц наосліп |
| вікно аналізу | оцінити RT60 (MLE) і тримати **30–50 мс** прямого звуку з ранніми відбиттями | фіксоване вікно, виправлене 15.09 (відкривається за 100 мс до приходу) |
| синтез фільтрів | найменші квадрати, **різати піки й не піднімати провали** гребінки, щоб не палити підсилювач | смугові підсилення з гейтом за довірою; правило «не піднімати провал» окремо не сформульоване |
| захват | `VOICE_RECOGNITION` або `UNPROCESSED`, 48 кГц, нативний бік | ✅ так і зроблено |
| затримки | GCC-PHAT для TDOA | ✅ так і зроблено |
| свіп | 20 Гц … **22 кГц** | 20 Гц (саб від 15) … 20 кГц, до проби каналу |
| рівень | MEMS-капсулі **нелінійні на високому тиску** (THD) | вимір іде на гучності 16 — власник 20.09: варто спробувати меншу, схоже на перевантаження |

### 4.3 Що з цього прямо пояснює сьогоднішній провал
- **Верх.** Документ каже: верхній спад — частина *теоретичної огинаючої* дифузного поля, а не персональна добавка
  мікрофону; персональне — лише гострі резонанси капсуля. Наша таблиця монтажу додає широкий +7…+10 дБ, чого вимір
  не підтверджує; правило «провал у всіх каналах = мікрофон» відсіяло б її на цьому апараті.
- **Низ.** Якір cabin gain дійсний у закритому салоні. На стенді салону немає, тож очікуваний підйом не настає, і
  дефіцит списується на капсуль — звідси насичення (+52.8 дБ на 20 Гц = стеля формули).
- **Оцінка.** Документ бере спільне (усереднення), ми беремо найкраще (максимум). Максимум за визначенням тягне
  оцінку в бік найгучнішого каналу, а не в бік того, що спільне.

### 4.4 Що це означає для наших правок (черга)
1. Насичення = невідомо (нуль + рядок у звіті) — прибирає брехню на низу.
2. Верх таблиці монтажу застосовувати лише за підтвердженням у **всіх** каналах; інакше не чіпати.
3. Перейти від «найкращого каналу» до спільної частини (логарифмічне усереднення з відкиданням мертвих каналів)
   — більша зміна, окремим кроком.
4. Перехідну частоту й нахил cabin gain брати з обраного кузова, а не тримати 80 Гц / 6 дБ на октаву.
5. Спробувати вимір на меншій гучності (перевантаження MEMS) і порівняти з 16.
