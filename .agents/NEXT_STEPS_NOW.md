# ▶️ ДЕ Я ЗУПИНИВСЯ І ЩО ДАЛІ (жива нотатка, 21.09.2026, кінець дня)

> Ця сторінка існує для того, щоб після автоматичного компакту одразу знати наступний крок.
> Оновлювати після КОЖНОГО кроку. Повний контекст — `.agents/HANDOFF.md`, останній зріз.

## 🔴 ЗАВТРА ВДЕНЬ — план власника (сказано 21.09 на закритті сесії)
1. **Свіпи** з новою математикою: калібрування мікрофона наново (в пам'яті апарата **стара** крива),
   далі вимір салону. Гучність виміру — спробувати меншу за 16 (перевантаження MEMS).
2. **Живий звук** — власник слухає й розповідає враження.
3. **Поміряти те, що ніколи не мірялось**: підсилення саба (+2 дБ для Harman — **стала в коді**,
   `synthesizeAutoEq16`; +3 Atmos, +5 Bass Heavy, 0 Vocal і Flat) і стеля підйому (+3 дБ до
   округлення, індекс 8 — на 125 Гц синтез упхався саме в неї). Рішення: міряти чи віддати людині.

## 🧾 БОРГИ, які власник назвав уголос (21.09) — ✅ ОБИДВА ЗРОБЛЕНО ВНОЧІ, ЧЕКАЮТЬ ПЕРЕВІРКИ ЗВУКОМ
- ✅ **Запуск плеєрів** — знайдено дві причини, обидві виправлені й стоять на апараті (коміти
  `49a9bc5`, `1e68ac0`). **Записування:** на завантаженні питалося «чи ще грало?», а останнє, що
  трапляється перед вимкненням, — платформа ставить на паузу, тож відповідь завжди «ні» (у чотирьох
  завантаженнях поспіль «nothing was playing»). Тепер пауза звіряється з моментом старту машини:
  ближче двох хвилин до нього — це вимкнення, годину тому — людина. **Запуск:** «є приймач
  медіакнопки» вважалося успіхом, і браузер-сервіс (єдина сходинка, що піднімає мертвий процес) не
  пробувався ніколи. Тепер драбинка з перевіркою кожної сходинки: сесія → браузер-сервіс → відкрити
  сам застосунок → сесія ще раз → медіакнопка. **Перевірено на дроті:** підняло застосунок і він
  заграв. ⚠️ Але підняло **радіо**, чого робити не можна — виправлено окремо: радіо більше не
  записується як ціль узагалі (одне місце вирішує).
  ➕ **Дописано за наказом власника** («рядом лежить DefaultAppsChanger, він це робить ідеально,
  але для рутованих; можеш підглянути»): чекаємо, поки система справді стартувала (foreground із
  `sys.qf.current.activity`, без рута), після відкриття застосунку **стежимо за появою сесії й
  просимо грати до п'яти разів через 1.5 с**, повертаємо екран туди, де він був (або на лаунчер), і
  **чекаємо мережу** для стрімінгових плеєрів (список, бо дозвіл INTERNET має майже все). Спотіфай у
  списку, але його особливості — окрема пісня на день.
  ⬜ **Лишилось перевірити зі справжнім плеєром і звуком** — це вдень.
  ⚠️ Платформний білий список сну застосунок не читає — Settings лише відкриває
  `com.qf.carsettings/.activity.FactorySleepWhiteListActivity`; wDSP у той список сам не пише, тож
  після сну працює лише те, що переживає сон.
- ✅ **Перемикач «Накривати статус-бар»** зроблено (коміт `eefff69`), типово **вимкнено**: вікно
  заставки — і позичене в смужки, і власне — починається під статус-баром і на стільки ж коротше,
  бекдроп малюється від `usableTop`. Оверлей не перехоплює того, чого не накриває, тож годинник,
  мережа і «недавні» лишаються живими. Увімкнено — стара повноекранна поведінка.
  ⬜ **Подивитись очима на апараті** — вдень.

## ✅ Зроблено 21.09 (усе закомічено, нічого не запушено)
1. Обидві правки виміру: насичення = невідомо; верх конструктиву лише за підтвердженням.
2. Подвійне лікування отвору прибрано; матриця перевіряється в обидва боки.
3. Конструктив і розміщення зведено в один клас `MicProfile` (порядок довіри: людина → однозначний
   висновок з місця → вимір). Карта — `ROOM_CALIBRATION.md` 24-quater.
4. Капсулю віддається лише **вузьке й спільне**; канали усереднюються в дБ, жоден не викидається;
   **cabin gain більше не припускається**. Числа й пресет до/після — `ROOM_CALIBRATION.md` 24-quinquies.
5. Виміри більше не стирають одне одного (тека на прохід, історія кривої, дані 14.09 і 20.09 у
   `.agents/measurements/`). Хостовий харнес `test_miccal.cpp` — у репозиторії.

## Стан (звірено 22.09, ~05:00)
- Гілка `kostyfmat_mod`, HEAD `d99f1b5`, дерево чисте, **не запушено 87 комітів**.
- **На апараті стоїть свіжа збірка** (versionCode 25, встановлено ~04:5x): нова математика виміру,
  `MicProfile`, архів вимірів, виправлений запуск плеєрів, перемикач статус-бару. Апарат
  перезавантажено, сервіс живий, логер пише в `0043`, радіо вимкнене.
- ⚠️ **Крива мікрофона в пам'яті апарата ще стара** — потрібен новий прохід калібрування.

## ▶️ Коли повернемося — з чого починати
1. Свіпи й живий звук за планом вище; зібрати враження власника.
2. Перевірити обидва борги очима й звуком: плеєр (не радіо!) грає → ребут → має піднятися сам;
   заставка з перемикачем в обох положеннях.
3. ГАЛА перевірити на дорозі: розгін — гальмування — рівень має повертатися.
4. Прошивка UIS8581, коли докачається — порівняти з еталоном 7862.
5. Борг звуку з 15.09: проба каналу вище 22 кГц. BitPerfect — у боргах.
