id=wDSP_bootlog
name=wDSP boot logger (read-only)
version=v1
versionCode=1
author=kostyamat
description=Waiting for the first boot. One folder per boot in /data/local/tmp/bootlog; reads logs only, changes nothing.
